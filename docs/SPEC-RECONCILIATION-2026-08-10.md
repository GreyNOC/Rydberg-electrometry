# Spec reconciliation — 2026-08-10

Four editors updated the normative specs to match the audited code; four independent auditors verified every claim by running it.

## spec-02-radial — mostly-faithful

applied 10, rejected 0

### discrepancies

- REQUEST 1 — the claim that the paragraph's relative figure is correct does NOT verify. Request (1) states "'the two differ by 2.5e-6 relative' is correct and is what makes the absolute figures visibly inconsistent". Measured: |δa₄|/|a₄| = 1.7400e-06 / 0.81633314 = 2.1315e-06, not 2.5e-6 — the printed figure is ~17 % high. The rest of the request (1.74e-6, ≤2.53e-7, max at r = 2/a₂ = 1.04 a₀, verbatim A4_L1_NOTE) verified exactly. I did NOT transcribe 2.5e-6 forward: writing it beside the new '2.13e-6 relative' derivation would have re-created the same internal inconsistency the request is fixing. Corrected in place to 2.13e-6 with the superseded figure disclosed; the argument that the relative statement exposes the wrong absolute figures is preserved (a ~2e-6 relative difference on a₄ ≈ −0.816 is δa₄ ≈ 1.7e-6, not 8e-8).
- NOT A REQUEST, found during verification — spec §6 B12's check value '≤1.5e-8 @ ν=20' does not reproduce. Measured 5.04e-8 on the shipped code, invariant under h (1e-3, 5e-4) and r_inner (1e-2, 1e-4), so it is hyperu's own error rather than a grid artifact. The TOLERANCE (rel ≤ 1e-6) is met either way and was left untouched; only the parenthetical check value was updated, with the superseded number kept visible.
- NOT A REQUEST, found during verification — spec §2.4's hyperu error table does not reproduce on the installed scipy 1.17.1 (ν = 15: 7e-11 printed vs 1.5e-10 measured; ν = 20: 1.5e-8 vs 3.5e-8; ν = 30: 8.2e-4 vs 1.8e-3; ν = 35: 1.7e-1 vs 0.49; ν = 40: 4.3e+1 vs 93). Resolved by adding a labelled scipy-1.17.1 row and declaring it binding, not by deleting the original.
- NOT A REQUEST, found during verification — §6, the header note and the footer all pointed at tests/test_spec02_benchmarks.py, which does not exist. The benchmarks live in tests/test_radial.py (B1–B14, B16) and tests/test_dipoles.py (B15).
- NOT A REQUEST, found during verification — §2.1's μ = (M − mₑ)/M contradicts the shipped μ = 1/(1 + mₑ/M) (src/rydsim/radial.py:390), which is what binding ruling R-10 in 00-conventions.md mandates. Difference measured at 4.17e-11 for Rb-85 (below every tolerance here), but the spec was teaching the superseded form; added a pointer to R-10.

### unsupported_claims

- §5 API block, coulomb_wavefunction contract line: "the hydrogen benchmarks B1-B7/B13-B14 and the B12 cross-check run through this + radial_matrix_element". FALSE for B13 and B14 as shipped. tests/test_radial.py::test_kaulakys_matches_gordon_hydrogen (B13, line 165) calls only radial_me_gordon and radial_me_kaulakys — no wavefunction is solved at all. tests/test_radial.py::test_outer_cutoff_adequacy_b14 (B14, line 148) calls R.radial_wavefunction(RB87, ...) — the Rb-87 model-potential path, not coulomb_wavefunction and not hydrogen. The claim is copied faithfully from the shipped docstring at src/rydsim/radial.py:407-409, so the error originates in the code, but it is now a spec claim too, and it contradicts this same document's new §6 note ‡, which correctly scopes the engine route to B1–B6, B7 and B12.
- §7.3 INTEGRATOR RULING: "The band 20 < ν ≤ 25 measures ~1e-5 — ten times worse than the tolerance B12 asserts". Measured on the installed scipy 1.17.1 via rydsim.radial.hyperu_hydrogen_error: 5.48e-6 (ν=25, l=0), 4.71e-6 (l=1), 5.72e-6 (l=2) — max 5.7e-6, i.e. 5.7× B12's 1e-6, not 10×. The ruling's own justification table three lines above prints 5.5e-6 at ν=25, so the prose contradicts the table it cites. Identical wording ships in src/rydsim/radial.py's whittaker_u docstring and in 00-integrity-audit.md refusal #6, so the spec is faithful to the code's stated rationale — the rationale itself is ~2× overstated. The ruling still stands on the measurement (5.7× outside contract is sufficient); only the multiplier is wrong.
- §6 B12b Tolerance cell: "shape rel ≤ 1e-6 (measured ≤3.0e-11)". The bound does not hold. Measured shape deviation after fitted scale: 2.89e-11 (ν=10.5, l=0), 1.14e-11 (l=1), 3.04e-11 (l=2) — the l=2 case listed in the same row already exceeds it — and 3.59e-11 at (ν=12.5, l=0), which is the shipped test's own second parametrization in tests/test_radial.py:261. The reproducible bound is ≤3.6e-11.
- §6 B12b prints offsets 6.6e-4 (l=1) and 3.0e-3 (l=2) at ν = 10.5, and §2.4 prints the r_inner = 1 a₀ → 3.7e-5 row, as measured facts. All three reproduce exactly (I measured 6.62e-4, 2.99e-3, 3.74e-5), but none is asserted by a shipped check: tests/test_radial.py::test_b12_noninteger_nu_shape_and_norm_offset is parametrized only over (10.5, 0, 7.3e-6) and (12.5, 0, 4.4e-6). House rule "every printed number is reproducible from a shipped check" is not met for these three. The editor disclosed this as a code change request — correctly.
- §6 note ‡ mechanism: "the centrifugal term (2l+1/2)(2l+3/2)/x² drives h²g/12 → 1, f = 1 − h²g/12 loses meaning". Computed from the shipped g (src/rydsim/radial.py:307-308) at r_inner = 1e-5, l = 2, h = 1e-3: h²g/12 = 0.21, i.e. f = 0.79, not → 1 (at 1e-4 it is 0.021). Direction, l-scaling and the measured degradation (B3: 2.73e-11 at 1e-4 → 5.39e-8 at 1e-5) all reproduce; the magnitude of the stated mechanism is ~5× overstated. Explanatory only — no printed number depends on it.
- §2.4 mechanism, minor: the norm offset is attributed to "the Whittaker function is irregular at the origin (W ~ r^{−l} as r → 0)". For the l = 0 row used as the demonstrator of cutoff dependence (7.3e-6 → 3.7e-5 when r_inner moves 1e-4 → 1 a₀), r^{−0} = 1 — the orbital is finite at the origin, merely non-vanishing, so no irregularity is involved. The mechanism is correct for l ≥ 1 and the measured l-scaling (7.3e-6 / 6.6e-4 / 3.0e-3) is exactly what r^{−l} predicts; for l = 0 the accurate statement is that u does not vanish at r → 0, so a truncated grid loses norm.
- §5 API block, model_potential_defect line types the first argument AtomParams. The shipped signature is model_potential_defect(species: Species, ...) at src/rydsim/radial.py:506; rydsim.atom exposes Species and has no AtomParams. Propagated from the block's pre-existing radial_matrix_element_consensus(species: AtomParams, ...) line rather than newly invented, so it is a consistent-with-the-doc error, not a new one.

### problems

- INTERNAL INCONSISTENCY (§6, introduced by this edit): the new note ‡ r_inner table prints, at the shipped default r_inner = 1e-4, B2 = 2.9e-12 and B6 = 1.9e-9. The B2 and B6 rows a few lines above still read "(measured 8e-13)" and "(measured 1.5e-8)" and were NOT marked ‡. I reproduced 2.93e-12 (B2) and 1.91e-9 (B6) as shipped — off from the printed figures by 3.7× and 8×. The editor marked only B1 and B7 with ‡, so §6 now contradicts itself in two rows. B5's "measured 5.5e-10" does reproduce exactly (5.51e-10); B3 and B4 print no measured value.
- INTERNAL INCONSISTENCY (§5 vs §6 note ‡): §5 says B13 and B14 run through coulomb_wavefunction; §6 note ‡ says the engine route covers B1–B6, B7 and B12. The code agrees with §6 note ‡, not §5 (see unsupported_claims item 1).
- CROSS-DOC POINTER LEFT STALE: spec 02 now states in three places that tests/test_spec02_benchmarks.py "was never created" and corrects the pointer to tests/test_radial.py + tests/test_dipoles.py — verified, the file does not exist and B1–B14/B16 are in tests/test_radial.py while B15 (B15_RADIAL_BAND_A0 = (5.45, 5.70), tests/test_dipoles.py:76) is in test_dipoles.py. But 00-integrity-audit.md R4's mitigation column, which sits ABOVE spec 02 in the precedence order and was edited in this same pass for the fence change, still reads "Port the harness as `tests/test_spec02_benchmarks.py` before any release". The name was not amended there.
- NO TOLERANCE WAS WEAKENED — verified cell by cell against git show HEAD. Every §6 Tolerance cell is byte-identical except for parenthetical *measured* values: B1 "rel ≤ 1e-7", B7 "∈ [8, 32]", B12 "rel ≤ 1e-6" all unchanged. The only threshold movements in the diff are 25 → 20 (§7.3 item 3, §4.4 pitfall 1, §5 whittaker_u contract), which is STRICTER and is the sanctioned change, plus ValueError → IntegrityError, which is a type correction matching src/rydsim/radial.py:834-841. B12's scope narrowed from "ν ≤ 20" to "integer ν ≤ 20", but new row B12b picks up non-integer ν at the SAME 1e-6 shape tolerance, so net coverage increases; and the narrowing corrects a genuine overclaim — the non-integer Seaton-vs-unit-norm offset is a real 3.0e-3 effect at l = 2 that the unqualified "agree" claim would have concealed.
- NORMATIVE HISTORY WAS AMENDED, NOT REWRITTEN. Every superseded value is quoted at the point of replacement: §3.3 block-quotes "δa₄ = 8e-8 perturbs Z₁(r) by < 4e-8" and discloses "this paragraph previously said 2.5e-6"; §2.4 keeps the original session-harness error row as a labelled table row rather than deleting it, and preserves the "≤2e-13 (ν ≤ 10)" support verbatim while scoping it to integer ν; §6 B1/B7/B12 keep the old check values visible; §2.1 keeps the ion-core form μ = (M−mₑ)/M and marks it superseded; §7.3's amendment block states "both of which read ν > 25". Style note only: §7.3 item 3's own text was edited in place (25 → 20) rather than kept verbatim with a strikethrough the way 00-conventions.md R-17 does it — the superseded value survives in the adjacent block, so nothing is lost, but it deviates from the pattern doc 00 demonstrates.
- VERBATIM CONSTANT MATCHES EXACTLY. The §3.3 block quote of rydsim.radial.A4_L1_NOTE is byte-identical to the shipped constant — confirmed by string equality (`A4_L1_NOTE == spec_text` → True), not by eye. C5E_CONVENTION_TENSION is not quoted in spec 02 (it belongs to docs 00/03), so nothing to check there.
- NO CONTRADICTION WITH 00-conventions.md. §2.1's new pointer aligns spec 02 with binding ruling R-10 (00-conventions.md:308-312) and with the code: src/rydsim/radial.py:390 computes mu_mass = 1.0/(1.0 + M_E/(species.mass_u*AMU)). Measured for Rb-85: R-10 form 0.999993539456, ion-core form 0.999993539414, difference 4.174e-11 — matching the spec's printed "4.2e-11" and R-10's own "≈ 4e-11"; 1−μ = 6.4605e-6 matches the retained "6.46e-6". This edit CLOSES a pre-existing spec-vs-code contradiction rather than creating one.
- EVERY OTHER SUBSTANTIVE NUMBER REPRODUCED. §3.3: |δa₄| = 1.7400e-6, relative 2.1315e-6, r_peak = 2/a₂(l=1) = 1.0372 a₀, r²e^{−a₂r} = 0.1455885, max|δZ₁| = 2.5332e-7; old figures wrong by 21.75× and 6.33× (printed 21.8× / 6.3×); ARC-reading consequences — consensus ME 2510.9114671114776 → 2510.911467111315 = 6.484e-14 relative, model_potential_defect(RB87,12,1,1.5) shift 4.695e-8. §2.4 scipy-1.17.1 row: 6.17e-13 / 1.47e-10 / 3.51e-8 / 5.48e-6 / 1.89e-4 / 1.85e-3 / 0.495 / 92.7. §2.4 offsets 7.29e-6 / 6.62e-4 / 2.99e-3 and 3.74e-5 at r_inner = 1 a₀; retained integer-ν support independently confirmed (worst |∫u²dr − 1| = 3.9e-15 over ν = 3..10, l = 0..2). §6 B12: 2.59e-11 / 2.38e-10 / 5.04e-8, and the 5.04e-8 is invariant across h ∈ {1e-3, 5e-4} × r_inner ∈ {1e-2, 1e-4} (5.036e-8 … 5.043e-8), confirming it is hyperu's own error and that "≤1.5e-8 does not reproduce" is correct. §6 note ‡: all 26 table entries reproduced, including B7 ratio 1.018 → "1.02" and 16.293 → "16.29". §6 MSD94 note: all eight residuals reproduced (Rb +3.2511e-4 / +2.1817e-3 / +9.9803e-4 / +8.4692e-4; Cs +5.6358e-4 / +5.3816e-3 / +1.3190e-3 / −1.2656e-4), DEFECT_BAND = 1.2e-2 and DEFECT_N = 12 read from tests/test_radial.py:558-559; a₃(l=0) doubling moves the consensus ME 3.85e-7 with the A−B spread 2.0177e-6 → 2.4026e-6 (printed 2.02e-6 → 2.40e-6), a₄(l=1) sign flip 6.21e-8. §7.3/§4.4/§5 code facts: WHITTAKER_NU_MAX = 20.0 (radial.py:115), IntegrityError on ν > 20 and on any non-finite sample (radial.py:834-856), _whittaker_u_unguarded at radial.py:780, non-finite regime confirmed for non-integer ν ≳ 11 (first NaNs at ν = 11.5), and grep confirms neither whittaker_u nor coulomb_wavefunction has any caller in src/ outside radial.py itself. All named tests exist and pass; tests/test_radial.py 95 passed; full suite passes (1 xfail).

### code_change_requests

- tests/test_radial.py::test_coulomb_route_refuses_above_nu_25 is now a misnomer — the fence is 20 (WHITTAKER_NU_MAX), and the test exercises nu = 30. Rename to ...refuses_above_nu_20 (or ..._above_the_fence) so the test name cannot be read as re-asserting the superseded 25.
- tests/test_radial.py::test_hyperu_fence_r4_binding_form still says 'the nu>25 fence may have become over-conservative' in its assertion message. The reasoning is right (a future scipy that makes ν = 25 accurate should trigger a revisit), but the wording names the old fence; update to reference WHITTAKER_NU_MAX = 20 with ν = 25 as the probe point.
- Spec 02 §6 B12b now prints measured norm offsets for l = 1 (6.6e-4) and l = 2 (3.0e-3) at ν = 10.5, but test_b12_noninteger_nu_shape_and_norm_offset is only parametrized over (10.5, l=0) and (12.5, l=0). Per the house rule that every printed number is reproducible from a shipped check, add (10.5, 1, 6.6e-4) and (10.5, 2, 3.0e-3) to the parametrize list, plus an r_inner = 1 a0 case pinning the l = 0 offset at 3.7e-5 (that row is what identifies the offset as a cutoff artifact and is currently only asserted in a docstring).
- Spec 02 §2.4's binding hyperu row is asserted at ν = 20/25/30/35 by the R4 test; ν = 15 and ν = 40 are printed but unasserted. Consider extending test_hyperu_fence_r4_binding_form to cover them, or drop those columns from the binding row.

## spec-03-angular — mostly-faithful

applied 12, rejected 0

### discrepancies

- QUALIFICATION, not a refutation — request (3), '§5 wigner_3j contract is now TRUE UNCONDITIONALLY in j': true of the kernel rydsim.wigner.wigner_3j (verified: wigner_3j(300,300,300,0,0,0) = 0.0020175058555708635, no raise), but §5 is headed `rydsim/angular.py`, and rydsim.angular.wigner_3j / wigner_6j still raise rydsim.provenance.IntegrityError for any j > 150 (_require_certified, _J_FLOAT_CERT_MAX = 150.0). Writing the unqualified sentence into §5 would have made the spec false about the module §5 documents. I wrote the accuracy contract as unconditional and added a separate 'Domain note (implementation, not accuracy)' for the wrapper's j <= 150 policy ceiling, with the exact oracle named as the opt-in route. Flagging so the parent can confirm this is the intended reading.
- NOT a discrepancy in any request, but a stale figure I could not fix (outside docs/spec/): §4.3.4's now-withdrawn 4.2e-12 is still asserted-adjacent in a test docstring — tests/test_angular.py:914. Listed under code_change_requests.
- NOT a discrepancy in any request, but a cross-document inconsistency I am scoped out of: 00-integrity-audit.md §3 refusal #13 still reads 'Generic (non-rank-1) Wigner symbols at j > 30 through the float path — route to the exact-rational oracle or refuse'. The shipped code takes that route AUTOMATICALLY at every j and rank via the round-off estimator, so the refusal is now discharged by the library rather than left to the caller. spec 03 §4.3.4 now says so explicitly; the audit doc (owned by another agent in this pass) may want the matching wording.

### unsupported_claims

- §7.9: "The residual real limitation is *cost*: the exact path uses big-integer arithmetic, which is why `rydsim.angular` still declares a j ≤ 150 domain (§5)" — the causal claim is not supported by the code. src/rydsim/angular.py:38-41 and §5's own Domain note give a different rationale ('no physical state in this simulator carries j > 150, so a request past the ceiling is a caller bug'), and the shipped IntegrityError message (src/rydsim/angular.py:124-130) gives a third: 'exceeds the certified float-path range j <= 150 (spec 03 SS4.3.4 / audit refusal #13)'. Nothing in src/, tests/, or docs/AUDIT-2026-08-10.md ties the ceiling to big-integer cost. Measured cost of one uncached exact-routed 3j: 0.30 ms at j=50, 1.68 ms at j=150, 11.0 ms at j=300, 71.6 ms at j=600 (all lru_cached thereafter) — real, but not obviously the reason for a fence at 150.
- §4.3.6: `exact_to_float` "still convert[s] to the nearest representable value". Measured against a 60-digit Decimal reference on the exact Fraction: rel err 8.4e-16 at j=100, 1.6e-14 at j=300, 3.7e-14 at j=500, 5.3e-14 at j=1000, 1.2e-13 at j=2000. That is ~L·ε accumulation, not 1-ulp nearest-representable. FAITHFUL to the shipped docstring (src/rydsim/wigner.py:176-188 says 'convert exactly to the nearest representable value'), so this is an inherited overclaim, not a new fabrication — but it is the mechanism by which §4.3.4/§5's '≤ 1e-12 UNCONDITIONALLY in j' would eventually fail (extrapolating, around j ~ 1e4). Every j any RydSim caller reaches has ~4 orders of margin.
- §6: the retained-grid bullet "(b) 2000 6j of type {l j ½; j′ l′ 1} (l ≤ 150; bare-float max err **3.0e-13**, reproduced)". Grid (b) is tests/test_angular.py:938-963, which calls the SHIPPED `wigner_6j`; I measured that 662 of the 898 non-zero symbols of that type route to the exact-rational path, so grid (b) does not measure the bare float path. The 3.0e-13 figure is a bare-float exhaustive-sweep result (I independently reproduced 2.9847e-13 at (138, 138.5, 0.5, 138.5, 139, 1.0), matching docs/AUDIT-2026-08-10.md:262) — the figure is real, its attribution to grid (b) is not.
- §4.3.4: "re-sweeping a different pseudo-random generic grid at j ≤ 60 already reaches ~8 × 10⁻⁴". Directionally true and conservative — my own independent 20k-draw bare-float sweep at j ≤ 60 reached 5.6e-3 at 3j(55, 59, 78; 8, 3, −11) — but the document specifies no grid, seed, or sampling rule, so the number cannot be reproduced from the spec. It is a new undocumented-grid figure introduced in the same paragraph that withdraws 4.2e-12 for being an undocumented-grid figure. Low severity (hedged with '~', used only qualitatively).
- §4.3.6: "sympy stays a test-suite-only dependency". True as to imports (grep confirms: no sympy import anywhere in src/rydsim/, only in tests/test_wigner.py:17-19 and tests/test_angular.py:155-157), but sympy is declared NOWHERE in pyproject.toml — not in `dependencies`, not in the `dev` extra. A fresh `pip install -e .[dev]` cannot collect tests/test_wigner.py at all. 'test-suite-only dependency' reads as 'declared under dev'; it is undeclared.

### problems

- NO BENCHMARK TOLERANCE WAS WEAKENED. `git diff` on spec 03 touches zero rows of the B1–B32 table. Every tolerance change moves STRICTER: §4.3.4's 'Pytest tolerance: 1e-9 on rank-1 and generic j ≤ 20' → '1e-12 on the rank-1 sweep and the generic 3j/6j grids'; §6's new grids assert 1e-12; and the old escape hatch '… tested against the oracle at 1e-3' was DELETED, not retained. The lifted prohibition ('do NOT assert 1e-9 on generic j ≤ 60') is justified — the shipped generic 3j grid to j = 150 measures 5.4e-14. I spot-verified retained rows independently: B1 = 0.408248290463863 = 1/√6; B4 shipped −0.07001736920773229 vs −√(51/10403), bare-float err 4.44e-14 (its '(float err 4.4e-14)' parenthetical still true); B5 = 1/√246; B6 = 1/6; B31 = 1.0380753376693606 inside 1.038 ± 0.01.
- HISTORY WAS AMENDED, NOT REWRITTEN. The defect-causing §2.7 sentence is quoted verbatim in the 'Defect provenance' blockquote; the withdrawn 4.2e-12 appears four times in the document, always labelled withdrawn; the generic-grid figures (8.2e-5 / 3.8e-7 / 5e-6) are retained and de-normativised rather than deleted; §6's superseded instruction is quoted and marked superseded; the header amendment block names all three changes, is dated, and explicitly says it supersedes the paragraph above rather than replacing it. This is the correct pattern.
- EVERY SUBSTANTIVE NUMBER REPRODUCES. Ran against the shipped code: f(g→e) = +0.6957718758472222, f(e→g) = −0.3478859379236111, ratio exactly −0.5, 2·f_abs + 4·f_em = 0.0 exactly, misuse (j_initial=0.5 with −ω) = −0.6957718758472222 i.e. exactly 2×; f(D1) = 0.34230346182213844, doublet sum 1.0380753376693606. 3j(100,90,80;0,0,0): bare float +2.521564662518986, exact −0.006783314088538622, ratio 371.7 (→372×), bound 1/√201 = 0.07053456158585983 exceeded 35.75× (→36×), shipped `wigner_3j` returns the exact value. Rank-1 worst case 3j(141.5,1,141.5;−½,0,½): bare-float rel = 2.9228e-11, estimate 1.178e-9 → routes. Test-printed figures reproduce exactly: rank-1 sweep 909 symbols worst 6.570e-14; generic 3j 122 symbols 5.420e-14; generic 6j 36 symbols 2.878e-14 — matching the spec's 6.6e-14 / 5.4e-14 / 2.9e-14. Full tests/test_wigner.py + tests/test_angular.py: 124 passed.
- ADVERSARIAL PROBE OF THE HEADLINE CLAIM — SURVIVES. The '≤ 1e-12 by construction' guarantee rests on a heuristic round-off estimator (src/rydsim/wigner.py:356, `est = 4*(1+scale)*eps/ratio`), not a proof, so I searched for the failure mode that would falsify it: symbols the estimator certifies (est ≤ 1e-12) whose true bare-float error exceeds 1e-12. Over 30,000 generic 3j samples at j ≤ 120, 2,019 were float-certified and ZERO violated the bound. The spec's formula `4(1+L)ε/R` and `FLOAT_PATH_TARGET_REL = 1e-12` match src/rydsim/wigner.py:356/410 and :73 exactly.
- API/DOMAIN CLAIMS VERIFIED: `rydsim.angular.wigner_3j_exact is rydsim.wigner.wigner_3j_exact` → True (same for wigner_6j_exact, exact_to_float); all three in both `__all__`s; `wigner_3j_exact(2,1,1,0,0,0)` → (1, Fraction(2,15)) → 0.36514837167011077 vs √(2/15) = 0.3651483716701107. `rydsim.angular.wigner_3j(151,1,151,…)` raises IntegrityError while the kernel `rydsim.wigner.wigner_3j(300,300,300,0,0,0)` = 0.0020175058555708635 with no raise — the §5 Domain note's split is exactly right, and the editor's decision to qualify the request's 'unconditional in j' for a section headed `rydsim/angular.py` was correct, not a dodge. Parameter is `j_initial` at angular.py:392/411/413; `steck_to_racah` genuinely still takes `j_lower` (angular.py:357) and §5 correctly left it alone. Non-(half)integer input still raises ValueError.
- INTERNAL INCONSISTENCY (the one real defect): §7.9 and §5 give DIFFERENT reasons for the same j ≤ 150 ceiling — §7.9 says big-integer cost, §5's Domain note says 'no physical state in this simulator carries j > 150, so a request past the ceiling is a caller bug'. Only the §5 version tracks the code. Worse, the message a caller actually sees (angular.py:124-130) still frames the ceiling as 'the certified float-path range', the accuracy framing §7.9 explicitly denies ('a policy ceiling, not an accuracy one'). An implementer reading §7.9 would write a different guard, and a user hitting the guard reads a rationale neither section endorses.
- PRE-EXISTING FALSE CLAIM LEFT IN PLACE (not introduced by this editor, but in a docstring they rewrote): §5's `wigner_3j` signature still reads `-> float | np.ndarray` with 'Scalar or broadcast arrays', and §4.3.5 still requires 'array frontends that broadcast with np.vectorize'. Neither entry point accepts arrays — `rydsim.angular.wigner_3j(np.array([0.5,1.5]), 1, 1.5, 0.5, 0, -0.5)` raises ValueError ('truth value of an array … is ambiguous') and the kernel raises TypeError ('type numpy.ndarray doesn't define __round__'). The editor replaced the Accuracy line of that exact docstring and left the false array line one line above it. `einstein_A` / `oscillator_strength` / `oscillator_strength_l` ARE genuinely vectorized, so the doc is not uniformly wrong — which makes the 3j/6j case easier to trust by mistake.
- CROSS-DOCUMENT: docs/spec/00-conventions.md:564-565 (§7 build order, step 3) still describes `rydsim.angular` as providing an 'exact-rational test oracle'. 00 sits ABOVE 03 in the precedence order, so a reader following precedence gets 'test oracle' from 00 and '§4.3.6 Exact-rational oracle — SHIPPED, not test-only' from 03. Low severity (00's step 2 describes the `rydsim.wigner` kernel correctly and matches spec 03), and the file is another agent's in this pass — but it is a live contradiction with the top-of-precedence document.
- THE EDITOR'S OWN DISCREPANCY NOTE #3 IS STALE — in the spec's favour. docs/spec/00-integrity-audit.md was amended in this same pass: refusal #13 (line 133-145) now carries 'IMPLEMENTED 2026-08-10 — by the *route to the oracle* arm, not the refusing arm' with the full routing description, and R24 (line 77) carries the matching amendment with the identical 6.6e-14 / 5.4e-14 / 2.9e-14 figures and the strikethrough of the retired rank-1-only rule. There is no audit-doc conflict left to fix.
- CODE-CHANGE REQUESTS (confirmed, outside docs/spec/): (1) tests/test_angular.py:914 still cites the WITHDRAWN '4.2e-12' as a measured figure — the editor flagged this correctly and it stands. (2) tests/test_angular.py:968-969 is stale in the opposite direction: 'Per SS4.3.4 this tolerance must NOT be extended to generic j <= 60' is the exact instruction spec 03 §6 now marks superseded for the shipped path — the editor did not flag this one. (3) tests/test_angular.py:941's '(measured max err 3.0e-13)' is a bare-float figure attached to a routed grid; the docstring should say so (see the §6 mislabel above). (4) sympy is an undeclared test dependency in pyproject.toml. (5) angular.py:124-130's IntegrityError message should be reworded to the policy rationale spec 03 §5/§7.9 now assert.
- BOTTOM LINE: faithful on everything that matters. No tolerance weakened, no history rewritten, no fabricated number — every figure I could re-derive matched to the digit, and the two figures the spec withdraws are independently confirmed non-reproducible by docs/AUDIT-2026-08-10.md:257-263. The residual issues are one genuine internal inconsistency (§7.9's cost rationale for the j ≤ 150 ceiling, contradicting §5 and unsupported by the code), one mislabelled figure attribution in §6, two overclaims inherited verbatim from shipped docstrings ('nearest representable value', 'unconditionally in j'), and one pre-existing false array-broadcast claim the editor left standing in a docstring they otherwise rewrote.

### code_change_requests

- tests/test_angular.py:911-914 — test_property_rank1_3j_vs_oracle's docstring still cites the WITHDRAWN figure: '(measured max err during spec prep: 4.2e-12)'. spec 03 §4.3.4 now records this as not reproducible; the exhaustive rank-1 sweep measures 2.9e-11 on the bare float path (worst case 3j(141.5, 1, 141.5; -1/2, 0, +1/2), verified rel = 2.9228e-11). Replace with the construction bound, or with the measured routed figure 6.6e-14 printed by tests/test_wigner.py. The companion figure at tests/test_angular.py:941 ('measured max err 3.0e-13' for the {l j 1/2; j' l' 1} 6j) DID reproduce — I measured 2.9847e-13 over an exhaustive l <= 150 bare-float sweep — and should be left alone.
- OPTIONAL, consistency only (not a defect): tests/test_angular.py:911-960 still asserts rel=1e-9 on the two rank-1 property grids while the shipped contract and tests/test_wigner.py assert 1e-12 (FLOAT_PATH_TARGET_REL). spec 03 §6 now documents the 1e-9 grids as a deliberately looser independent check, so this is self-consistent as written; tightening them to FLOAT_PATH_TARGET_REL would remove the two-tier explanation.

## spec-00-conventions — problems-found

applied 9, rejected 0

### discrepancies

- REQUEST 3 PREMISE WRONG (did not write it as given): the request says the zeeman row 'reads Depends on: atom, angular' and that zeeman now ALSO imports wigner and constants. Measured import set of src/rydsim/zeeman.py is {constants, provenance, wigner} — it imports NEITHER rydsim.atom NOR rydsim.angular, at module level or function-locally (AST walk incl. deferred imports; the only import lines are zeeman.py:73-75). Both atom-derived data (n*, fine-structure intervals) arrive as caller-supplied arguments to state_shift_hz/require_linear_regime/require_linear_dominates. I wrote the measured set and recorded atom as a data (non-import) dependency; writing 'atom, angular, wigner, constants' would have been an unverified claim.
- REQUEST 3 SECTION NUMBER: the request says '§8 dependency graph'; in this document §7 is the module dependency graph and §8 is Enforcement. I edited §7 (and separately appended to §8 for the new rulings' test hooks).
- REQUEST 3 CONSTANT LIST INCOMPLETE: the request names rydsim.constants (A0, E_CHARGE, M_E). The shipped line is 'from .constants import A0, E_CHARGE, H, M_E, MU_B'. I recorded all five, marking A0/E_CHARGE/M_E as the additions that carry the diamagnetic prefactor e²a₀²/(8 m_e h) — transcribing only three would have understated the edge.
- REQUEST 1 SUMMARY IMPRECISE (corrected, not rejected): 'the diamagnetic term is 10-30% of the returned shift over B = 0-412 G' — the fraction is B²-scaled, measuring 1.48%/4.32% at 60 G and 10.19%/29.69% at 412 G. Wrote the four regression-tested figures with their fields attached.
- BRIEF'S SUITE FIGURE INCOMPLETE: '604 passing / 1 xfailed' measures as 606 collected — 604 passed, 1 xfailed (tests/test_stark.py::test_rs_07_15_ponderomotive_strict), 1 skipped (tests/test_gui_smoke.py, no display), 0 failures. Recorded in full.
- PRE-EXISTING, NOT A REQUEST CLAIM (recorded in-document as intent-vs-measured, rows left unrewritten): §7 steps 8 (rydsim.vapor), 10 (rydsim.obe) and 13 (rydsim.validation) name modules that do not exist in src/rydsim — that work is spread across cell/doppler/spectroscopy, lindblad/eit/experiment, and objective/designer/tests. Two further measured deviations: rydsim.lifetimes imports {angular, atom, cell, constants, provenance, radial} (not 'dipoles, atom, constants' as the row states), and rydsim.stark imports rydsim.wigner directly. Reconciling those rows needs an owner decision about module naming, so I flagged rather than rewrote them.
- CODE-COMMENT COUNT MISMATCH (cosmetic, code frozen): src/rydsim/objective.py:285-286 says the species-name hack 'was duplicated in three modules', while tests/test_objective.py:193 and the four surviving call sites (dipoles, lifetimes, objective, radial) say four. I wrote 'four' in R-27, matching the test docstring and the call-site count.

### unsupported_claims

- R-27 (docs/spec/00-conventions.md:487-489): 'The species -> element mapping was **forked across four modules** as `sp.name.startswith("Rb")`' and the enumeration ending in '...objective's vapor-cell parameters'. MEASURED FALSE — it was THREE modules. `git show HEAD:src/rydsim/*.py` grep for `sp.name.startswith`: src/rydsim/radial.py:130,132 (`_potential_params_for`), src/rydsim/lifetimes.py:178 (`_element`), src/rydsim/dipoles.py:247 (A4_L1_NOTE gating). src/rydsim/objective.py had ZERO occurrences of any species-name test — `species_cell_parameters()` is NEW code written by the remediation, so objective is a new CONSUMER of `element_symbol`, never a fork site. The four current `element_symbol` call sites are not four pre-existing forks. Consequence: the shipped inline comment at src/rydsim/objective.py:285-286 ('that hack was duplicated in three modules') is CORRECT, and the editor's code_change_requests[0] would edit correct code into agreement with an incorrect ruling. The 'corroborating' docstring at tests/test_objective.py:193 was authored in the same pass and is not independent evidence — it carries the same error.
- R-27 (docs/spec/00-conventions.md:492-494): 'The form has **no refusal branch**: any species whose name does not start with "Rb" fell through to **Cs**, silently — a wrong number where the only correct answer is a stop.' MEASURED FALSE for two of the three sites. Only `lifetimes._element` did that (`return "Rb" if sp.name.startswith("Rb") else "Cs"`, HEAD:src/rydsim/lifetimes.py:178). `radial._potential_params_for` (HEAD:src/rydsim/radial.py:130-137) had an explicit refusal branch: `else: raise IntegrityError(f"no MSD94 model-potential parameters for species {sp.name!r}; refusing to guess (spec 02 §3)")` — and radial is the FIRST module the ruling names. `dipoles.py:247` was a gating condition (`if sp.name.startswith("Rb") and 1 in (state1[1], state2[1])`) whose only effect for a non-Rb species is that the A4_L1_NOTE provenance string is not appended — no Cs fallthrough, no wrong number. The editor's empirical check ('the superseded form classifies Na23 as Cs') exercises lifetimes' form only and was generalized to all sites. The ruling's NORMATIVE content (one source of truth, refuse rather than infer) is correct and well-supported; its recorded HISTORY overstates the defect.
- §7 shipped-tree note (docs/spec/00-conventions.md:598-600): 'steps 1-5 and 9 — `constants`, `provenance`, `wigner`, `angular`, `atom`, `radial`, `zeeman` — exist under these names and their `Depends on:` lines above are **measured import sets**.' FALSE for step 5. AST walk (module-level + function-local) of src/rydsim/radial.py gives {atom, constants, numerov, provenance}; the step-5 row reads only 'Depends on: atom (n*, `element_symbol`), constants'. The two missing edges are literal module-level imports at src/rydsim/radial.py:66 (`from .numerov import numerov_inward, numerov_outward`) and :67 (`from .provenance import IntegrityError`), and `rydsim.numerov` — a shipped file — appears nowhere in §7 or the §4 ownership map. Every other row in the claimed set verifies exactly (constants/provenance -> {}, wigner -> {provenance}, angular -> {constants, provenance, wigner}, atom -> {constants, provenance}, zeeman -> {constants, provenance, wigner}).
- §7 shipped-tree note (docs/spec/00-conventions.md:601-603): '`lifetimes` reaches `radial`/`angular` directly rather than only through `dipoles`'. Measured `lifetimes` imports {angular, atom, cell, constants, provenance, radial} — it does not import `dipoles` AT ALL (so there is no 'only through dipoles' path to contrast against), and it also imports `cell`, which the note omits. Lower severity: the step-7 row is explicitly flagged as intent-not-measured, so this is a mis-characterization inside a disclosed-uncertain note rather than a claim dressed as measured.
- R-28 (docs/spec/00-conventions.md:522-524): '`LadderConfig.species_defaults_in_use()` returns the subset of the six still at their Rb-87 values (**empty exactly when all six are overridden**)'. The parenthetical biconditional is FALSE of the shipped implementation. src/rydsim/experiment.py:120-122 compares VALUES (`getattr(self, k) == v`), not whether the caller supplied the field. Measured live: `LadderConfig(**species_cell_parameters(SensorDesign(species='Rb87', n=50, state='D5/2', rf_partner='P3/2'))).species_defaults_in_use()` -> `['element', 'isotope_fraction']` — non-empty despite all six being explicitly supplied with the correct Rb-87 values. (Cs-133 correctly returns `[]`, and the bare `LadderConfig()` correctly returns all six.) The shipped docstring is careful — 'A non-empty list **on a non-Rb-87 run** means the config is physically mixed' — and the spec drops that qualifier.

### verbatim_mismatches

- EDITOR'S REPORT OVERSTATES VERBATIM COPYING (the document itself is defensible). The report for the R-22 extension says 'Copied from the shipped constant src/rydsim/dipoles.py:339 C5E_CONVENTION_TENSION (not paraphrased)'. The spec text is NOT a verbatim copy: it is restructured into a two-row markdown table plus prose, with two figures added that do not appear in the constant (`2291.2 e·a₀ = 1.9426e-26 C·m` and `1774.8 e·a₀ = 1.5047e-26 C·m`) and one added arithmetic step (`short by 1.8859/1.3267 = 1.4215`, where the constant writes only 'short by 1.4215'). Every added figure verifies against a live run (Rb87: 1.942602e-26 C·m, 1.504733e-26 C·m; ratio-of-ratios 1.421533), so nothing is wrong — but 'copied, not paraphrased' is not what shipped. The document's own sentence, 'The verbatim provenance text ships as `rydsim.dipoles.C5E_CONVENTION_TENSION`', reads as a pointer to where the verbatim string lives rather than a claim about this paragraph, and docs/spec/09-validation-corpus.md:490 does reproduce the constant verbatim in a fenced block. So: report-accuracy defect, not a false statement in 00-conventions.
- EDITOR'S CITED LIVE FIGURE COMES FROM THE WRONG SPECIES (document is right). The report records 'radial 3622.79 a₀ (spread 6.3e-6)' from its re-run. That is Rb85 (measured 3622.7901035, spread 6.2587e-6). Rb87 — the species the fixture, the shipped constant and the test all use — measures 3622.7830469 with a three-method spread of 6.1101e-6, so the document's '3622.78 a₀ (three methods agreeing to 6e-6)' matches the code exactly. The three methods are model_potential / coulomb / kaulakys, confirming the 'three methods' wording.

### problems

- VERDICT SUMMARY: the numeric and physics content of this edit is exemplary — every measured figure I re-ran reproduces to the printed digit. The defects are all in RECORDED HISTORY and in one over-claimed 'measured' statement. No tolerance was weakened and no ruling's history was silently rewritten.
- NO TOLERANCE WEAKENING (checked directly). `git diff -U0 docs/spec/00-conventions.md` removes exactly 25 lines, all of which are re-written with additions; not one carries a tolerance. R-18's acceptance tolerances (|Tr-1| < 1e-12, Herm < 1e-10, eig > -1e-10; spec 09's 1e-10) are byte-identical. The C5e treatment explicitly preserves the fixture: docs/spec/09-validation-corpus.md keeps C5e at '1.37×10⁻²⁶ C·m | 5% rel' and states 'The expected value ... and the 5 % tolerance are NOT changed'. The sanctioned 25 -> 20 change is the hyperu/Whittaker fence and lives in docs/spec/02 (`rydsim.radial.WHITTAKER_NU_MAX = 20.0`), not in this document; 00-conventions makes no ν-fence claim, so there is no contradiction to reconcile.
- NO SILENT RULING REWRITE. R-17 keeps its superseded closing sentence ('Quadratic Zeeman and E×B remain out of scope.') verbatim with an inline supersession marker plus a dated amendment block — textbook amendment. R-22's body is untouched; only its TITLE was rewritten in place, and that is disclosed inline ('*(Title extended 2026-08-10; the ruling covers the class, not one paper.)*'). §6 gap 1 and the §7 rows were replaced without preserving prior text, but those are status/graph rows, not normative rulings — appropriate.
- R-17 AMENDMENT FULLY VERIFIED. Re-ran the physics: neglected/returned = 0.014843529 (60 G) and 0.101925568 (412 G) for n*=42.5, l=2, j=m_j=5/2; 0.043231963 (60 G) and 0.296859478 (412 G) for n*=44.9, l=0, j=m_j=1/2 — matching the printed 1.5 % / 10.2 % / 4.3 % / 29.7 % exactly. brentq crossing of the 5 % fence: 202.108 G ('202 G'). LINEAR_FENCE_FRACTION == DIAMAGNETIC_FENCE_FRACTION == 0.05 (src/rydsim/zeeman.py:85,90). The '~60 G' channel-1 threshold re-derives as 0.05·5e9/(1.399625e6·3) = 59.5 G. E7's 'B = 0-412 G' is at docs/spec/09-validation-corpus.md:279. The n* labels are real states, not round numbers: `n_star(Cs133, 45, 2, 2.5)` = 42.5337 (C9's 45D5/2) and `n_star(Cs133, 49, 0, 0.5)` = 44.9505 (E7's 49S1/2). Per-state fencing in `transition_shift_hz` (src/rydsim/zeeman.py:409-436) is as described.
- R-22 EXTENSION FULLY VERIFIED. Live `mu_rf(Rb87, (53,2,2.5), (54,1,1.5))`: stretched 2291.24918 e·a₀ = 1.942602e-26 C·m (+41.796 %), nist_pi 1774.79398 e·a₀ = 1.504733e-26 C·m (+9.835 %), radial 3622.783047 a₀ across three methods with spread 6.11e-6. computed/printed = 1.4179603 -> √2 to 0.265 %. Code-independent arm: 1615.88/1218 = 1.326667, 1.8859/1.326667 = 1.421533 -> √2 to 0.518 %. `MU_RF_CONVENTIONS == ('nist_pi','stretched')`; `pi_manifold_rms` appears nowhere in src/ (only tests/test_dipoles.py:470 asserting it is rejected, and the audit/spec prose describing its removal).
- R-28's SIX-FIELD INVENTORY VERIFIED (only the parenthetical is wrong — see unsupported_claims). src/rydsim/experiment.py:105-112 `_SPECIES_DEFAULTS` holds exactly {gamma_e: 2π·6.07e6, mass: 86.909 u, lambda_probe: 780.241e-9, lambda_coupling: 480.0e-9, element: 'Rb', isotope_fraction: 0.2783} — every value matches the ruling's text and the CRITICAL-1 wording at docs/AUDIT-2026-08-10.md:10-16. `species_cell_parameters` is importable from `rydsim.objective` and computes λ_c per state (480.125 nm for Rb87 50D5/2, i.e. not the nominal 480.0).
- ACYCLICITY CLAIM VERIFIED INDEPENDENTLY. AST walk over every file in src/rydsim (module-level plus function-local deferred imports), then DFS cycle detection: cycles = []. `constants` and `provenance` import nothing from rydsim; `wigner -> {provenance}`; `angular` and `zeeman` -> {constants, provenance, wigner}. The zeeman row is right where the request's premise was wrong: zeeman imports NEITHER `atom` NOR `angular` (only src/rydsim/zeeman.py:73-75), and `clebsch_gordan` is genuinely used at zeeman.py:207 in `mean_sin2_theta`. The editor was correct to refuse the request's wording here.
- §6 GAP 1 AND §8 VERIFIED. C9 is TIGHT (docs/spec/09-validation-corpus.md:545 defines TIGHT as ≤5 %, C-family) at 1 % rel (row at :472), and tests/test_zeeman.py:98 implements it against μ_B/h. All eight tests named in §8 items (vi)-(viii) exist and pass: test_neglected_over_returned_ratio_is_10_to_30_percent, test_diamagnetic_fence_raises_where_the_fs_fence_passes, test_diamagnetic_fence_is_armable_for_l_zero, test_e7_conditions_exceed_linear_fence (tests/test_zeeman.py); test_c5e_tension_note_digits_track_live_computation, test_sedlacek_tu_ratio_shows_sqrt2_without_rydsim (tests/test_dipoles.py); test_species_element_mapping_has_one_source_of_truth, test_ladder_config_reports_unoverridden_species_defaults (tests/test_objective.py).
- SUITE FIGURE IS ENVIRONMENT-CONDITIONAL BUT HONESTLY LABELLED. Full run here: 606 collected, 0 failures, 0 errors, 1 junit-'skipped' entry which is the xfail tests/test_stark.py::test_rs_07_15_ponderomotive_strict — i.e. 605 passed / 1 xfailed / 0 skipped, because a display was available and tests/test_gui_smoke.py ran. The document's '604 passed ... 1 skipped (headless GUI smoke)' is the same run under a headless environment and names the reason, so it is qualified rather than wrong. Worth a note only because the printed split does not reproduce on a machine with a display.
- RECOMMENDED FIXES (spec-side only; I edited nothing). (1) R-27: change 'forked across four modules' to 'three modules (radial, lifetimes, dipoles)' and drop objective from the fork enumeration, recording it instead as the new consumer the fix created. (2) R-27: scope the 'no refusal branch / fell through to Cs silently' sentence to `lifetimes._element`, and describe radial's prior form accurately (it refused, but with a duplicated mapping — which is still exactly the R10 pattern the ruling is about, so the ruling survives intact). (3) WITHDRAW the editor's code_change_requests[0]: src/rydsim/objective.py:285-286 is correct as shipped; the stale text is tests/test_objective.py:193-194, which should move from 'four modules' to 'three'. (4) §7 step 5: add `numerov` and `provenance` to radial's Depends-on line, or drop radial from the 'measured import sets' list; consider giving `rydsim.numerov` a build step since it is a shipped module absent from both §4 and §7. (5) §7 note: state that `lifetimes` does not import `dipoles` at all and does import `cell`. (6) R-28: replace '(empty exactly when all six are overridden)' with '(empty exactly when all six differ from the Rb-87 default VALUES; a correctly built Rb-87 config still reports element and isotope_fraction)', matching the shipped docstring's own qualifier.

### code_change_requests

- src/rydsim/objective.py:285-286 — the inline comment reads 'that hack was duplicated in three modules'; the shipped test docstring (tests/test_objective.py:193) and the four element_symbol call sites (dipoles.py:261, lifetimes.py:203-211, objective.py:287, radial.py:167) say four. Ruling R-27 as written says four. Suggest correcting the comment to four so the code and the normative register agree.
- OPTIONAL / owner decision, not a defect: spec 00 §7 still names rydsim.vapor (step 8), rydsim.obe (step 10) and rydsim.validation (step 13), which have no corresponding files in src/rydsim (that work lives in cell/doppler/spectroscopy, lindblad/eit/experiment, and objective/designer plus the test corpus). Either the modules get consolidated under the spec names or §7 rows 8/10/13 need rewriting against the shipped tree. I recorded the gap in §7 as an intent-vs-measured note rather than guessing the intended mapping. Related measured facts for whoever takes it: rydsim.lifetimes reaches radial/angular directly rather than only through dipoles, and rydsim.stark imports rydsim.wigner directly.

## spec-00-audit-09-corpus — mostly-faithful

applied 11, rejected 0

### discrepancies

- Request (4) says to add the Sedlacek row 'next to the existing R-22 Jing sqrt(2) entry' in 09 §9. THERE IS NO SUCH ENTRY. Spec 09 §9 has only 'Jing LO frequency beyond "6.94 GHz"' and 'Jing integration time behind the 780 pV/cm minimum field'; grep for 'R-22' and 'sqrt(2)'/'RMS' over 09-validation-corpus.md returns nothing in §9 (the Jing sqrt(2) lives in 09 §2.5 as the printed prefactor, and its ruling lives in 00-conventions.md §5 ruling R-22). ACTION TAKEN: I transcribed the requested row verbatim and placed it beside the two existing Jing rows; I did NOT fabricate an R-22 row to sit next to (I drafted one, then removed it — asserting 'RydSim never imports a literature Rabi/field value' as a §9 register entry was not part of any request and I had not verified it end-to-end). The 'same artifact class' link is instead made explicitly inside the new C5e blockquote. If the integrator wants a standing R-22 row in 09 §9, that is a clean follow-up edit.
- 00-integrity-audit.md §3 refusals #1 and #14 carry the SAME exception-type error that RULING A corrected for #6, and no ruling covers them. #1 says 'Quantum-defect energies below n_min_hard (Rb: 8, Cs: 12) — ValueError'; the code raises IntegrityError (verified: rydsim.atom.quantum_defect(RB87, 5, 0, 0.5) -> IntegrityError). #14 says 'Beterov fit paths outside 15 <= n <= 80 — ValueError'; rydsim.lifetimes raises IntegrityError('Beterov fit valid only for 15 <= n <= 80, got n=...'). This is MATERIAL, not cosmetic: IntegrityError.__mro__ is (IntegrityError, RuntimeError, Exception, BaseException) — it does NOT subclass ValueError, so an implementer following the spec text and writing `except ValueError:` would fail to catch either refusal. I did NOT edit these (outside the four requests and outside RULING A's stated scope: 'Applies to: spec 02 §7.3 and 00-integrity-audit.md refusal #6'). Requesting an integrator ruling to extend RULING A's exception-type correction to refusals #1 and #14; the fix is a docs-only edit in the same file.
- RULING A's characterisation '20 < nu <= 25 measures ~1e-5 (ten times worse)' is the shipped docstring's wording, but the measured value at the top of that band is 5.48e-6 on scipy 1.17.1 — 5.5x the B12 1e-6 contract, not 10x (the '~1e-5' rounding is what makes it 10x). NOT a defect and NOT re-litigated: the fence direction and the ruling are unaffected, and I transcribed the ruling's language verbatim while printing the exact measured table (6e-13/3.5e-8/5.5e-6/1.9e-4/1.8e-3/0.49/93) immediately alongside it so no reader is misled by the round number. Flagging only so the '10x' figure is not later hardened into a claim.
- Two register rows in my own document were left stale by the remediation and are NOT in the four requests; I amended both in place with dated AMENDED notes rather than leave the document self-contradicting, and both amendments are verified: (a) R4's self-check clause 'whittaker_u raises for nu > 25 regardless' (now 20, per RULING A); (b) R24's self-check 'enforce the rank-1-only certification rule for j > 30' (retired — rydsim.wigner now routes to the exact oracle automatically, so accuracy is 1e-12 by construction at every rank; measured 6.6e-14 / 5.4e-14 / 2.9e-14 worst-case vs the oracle). Flagging for integrator visibility since neither was explicitly commissioned.
- Not a discrepancy in the requests, but worth recording: the full `pytest` run exceeds the 2-minute tool timeout on this machine, so I could not re-confirm the '604 passing / 1 xfailed' headline end-to-end. I verified the four suites my edits describe instead — tests/test_dipoles.py, tests/test_radial.py, tests/test_wigner.py, tests/test_angular.py — 168 passed, 0 failed. (The single xfail lives in tests/test_stark.py:462 (RS-07-15), unrelated to anything in this pass.)

### unsupported_claims

- 00-integrity-audit.md refusal #6: 'benchmark B12 demands 10⁻⁶ pointwise, and 20 < ν ≤ 25 measures ~10⁻⁵ — ten times worse'. NOT REPRODUCIBLE. Measured on the shipped instrument (rydsim.radial.hyperu_hydrogen_error, scipy 1.17.1): ν=21 -> 9.29e-8, ν=22 -> 1.85e-7, ν=23 -> 6.72e-7, ν=24 -> 2.48e-6, ν=25 -> 5.48e-6. Three of the five integer ν in the cited band are INSIDE B12's 1e-6 contract (ν=21 is 10x BETTER than it), and the worst point in the band is 5.5e-6 — 5.5x the contract, not 10x. The band does not 'measure ~1e-5' anywhere. This also contradicts the correct table printed three lines below it in the same bullet ('5.5e-6 @ 25').
- 00-integrity-audit.md R20 self-check, by omission: the row states the old tripwire was 'inoperative, and the one table carrying an UNVERIFIED digit was unguarded', then installs the model_potential_defect tripwire and reports 'every corruption that B8 passed silently now breaches the band'. True of the 10 listed corruptions, but the shipped test carries an explicit 'Honest scope statement' (tests/test_radial.py:637-644) that the a4 rows are only weakly determined — 'Rb a4(l=1) has to move ~50 % before it breaches the band', which is why the mutation case is an O(1) sign flip rather than the real 2.1e-6-relative transcription gap. The specific UNVERIFIED digit R20 exists for is therefore still not guarded by the new tripwire; it is bounded (6.5e-14 on the ME, 4.7e-8 on the defect), which the Why column does state. The self-check reads as closing a gap the code says it does not close.
- 09-validation-corpus.md C5e Confidence cell: 'Reproduced by **no** published convention'. What is demonstrated is narrower — that neither member of the closed set rydsim.dipoles.MU_RF_CONVENTIONS ('nist_pi', 'stretched') reproduces it. The blockquote and the shipped C5E_CONVENTION_TENSION both correctly scope this ('neither published convention in rydsim.dipoles.MU_RF_CONVENTIONS (a closed set: ...)' / 'either published convention'). The header cell's unqualified form is a literature-wide claim that was not tested. Self-correcting because the cell points to the blockquote, but it should read 'neither implemented published convention'.

### problems

- P1 (MATERIAL) — The pass HARDENED an unreproducible measured claim instead of correcting it. Refusal #6's rationale sentence '20 < ν ≤ 25 measures ~10⁻⁵ — ten times worse' is false as a description of the band (see unsupported_claims #1). The fence itself is fine: 20 is stricter, ν=24 and ν=25 genuinely do breach B12's 1e-6 contract, and the direction is sanctioned. What is wrong is the RATIONALE, which is the thing the next implementer re-derives from. The same wording propagates to four places — docs/spec/00-integrity-audit.md:107, docs/spec/02-radial-wavefunctions-numerov.md:618, src/rydsim/radial.py:113 and :814-816 — and, worst, into the runtime message at src/rydsim/radial.py:837, which tells a caller refused at ν=20.1 that 'scipy hyperu carries ~1e-5 relative error above this' when the measured value there is ~9e-8, off by ~100x. The spec is FAITHFUL to the code here (it transcribes the code's own words), so this is a shared spec+code defect, not a spec-only one. The editor disclosed a weaker version of this in discrepancies_found #3 ('5.5x not 10x at the top of the band') and judged it 'NOT a defect'; that understates it — the issue is not rounding, it is that most of the cited band is inside contract. Fix: restate as 'the fence sits at the last integer ν whose measured error is 10x inside B12's contract (3.5e-8 at ν=20); the contract is first breached at ν=24 (2.5e-6) and the method collapses to 5.5e-6 at 25, 0.49 at 35', and correct the IntegrityError message text.
- P2 (MATERIAL, disclosed by the editor) — refusals #1 and #14 still say `ValueError`; the shipped code raises `IntegrityError`. Verified independently: rydsim.atom.quantum_defect(RB87, 5, 0, 0.5) -> IntegrityError; rydsim.lifetimes raises IntegrityError('Beterov fit valid only for 15 <= n <= 80, got n=...') at lifetimes.py:977-978, and that module's own docstring (lifetimes.py:75-76) already records 'audit SS3 item 14: Beterov fit paths outside 15 <= n <= 80 raise rydsim.provenance.IntegrityError'. IntegrityError.__mro__ is (IntegrityError, RuntimeError, Exception, BaseException) — it does not subclass ValueError, so an implementer writing `except ValueError:` from the spec catches neither refusal. THIS PASS AGGRAVATED IT: refusal #6 now states the general rule ('a refusal carrying an integrity reason is an IntegrityError') and labels the ValueError wording 'a mis-statement of this register's own rule', while #1 and #14 keep that exact mis-statement. Before the edit all three were consistently wrong; the document is now internally inconsistent. The editor flagged this and declined to edit, citing RULING A's stated scope — defensible, but the inconsistency is live and is a docs-only fix.
- P3 (MINOR, undisclosed) — R20's new self-check omits the shipped test's own scope caveat, so it reads as if the UNVERIFIED a4(l=1) digit is now guarded when tests/test_radial.py:637-644 says it is not at the relevant magnitude. See unsupported_claims #2. Recommend carrying the test's sentence into the row.
- P4 (MINOR, cross-document, likely outside the editor's scope) — 'integrator RULING A' is cited as binding in 00-integrity-audit.md (R4 and refusal #6) and applied in docs/spec/02-radial-wavefunctions-numerov.md §7.3, but it is NOT registered in docs/spec/00-conventions.md, which sits at the top of the precedence order. That file's 2026-08-10 amendment log (lines 12-21) enumerates this pass's rulings — R-17 (integrator ruling B), R-22 (integrator ruling C), R-27, R-28 — and never mentions RULING A or the Whittaker fence; grep for 'Whittaker'/'WHITTAKER' over 00-conventions.md returns nothing. Not a contradiction (its claim 'No benchmark tolerance was relaxed in this pass' remains true — I confirmed it), but rulings B and C of the same pass ARE registered there while A is not, so a reader working top-down from the authoritative register would not learn the fence moved. This is precisely the class of gap R26's own outstanding 'CI doc-link check' exists to catch.
- P5 (VERY MINOR) — C5e Confidence cell overreaches with 'Reproduced by no published convention' where the evidence supports 'neither implemented published convention'. See unsupported_claims #3.

### code_change_requests

- CI doc-link / cross-reference check (audit R26's remaining item, outside docs/spec/ so out of my scope). The two stale pointers in spec 09 are now fixed by hand, but nothing prevents the next one: add a CI check that spec cross-references of the form 'spec NN' / 'docs/spec/NN' for an owned quantity agree with the 00-conventions.md §4 ownership map. R26 is marked RESOLVED for the pointers and explicitly notes the CI check is still outstanding.
- Optional, and only if the integrator declines to extend RULING A by docs edit instead: if refusals #1 and #14 are meant to be catchable as ValueError by external callers, rydsim.provenance.IntegrityError would need to inherit from ValueError as well as RuntimeError. I recommend AGAINST this — the code is right and the spec text is wrong, so the correct fix is the docs edit named in discrepancies_found. Recording it here only so the alternative is on the record rather than actioned silently.

